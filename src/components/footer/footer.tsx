import React from "react";
import "./footer.scss";

import Logo from "../global/logo/logo";

const Footer = () => {
    return (
        <footer>
            <Logo className={"footer"} />
        </footer>
    )
};

export default Footer;
